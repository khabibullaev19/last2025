package pack06conditionalstatements;

public class ConditionalStatements {

    /*

        - Conditional Statement is: Conditions and if statements let you
          control the flow of your program - deciding which code run and
          which code is skipped.

          Think of it like real life: If it rains, take an umbrella, otherwise
          do nothing.

          Every if statement needs a condition that results in "true" or "false".
          This means "if" statements work hand-in-hand with "boolean" values.

          Most often, conditions are created by using comparison operators like below:
          a < b less than
          a > b greater than
          a <= b less than or equal to
          a >= b greater than or equal to
          a == b equal to
          a != b not equal to

          Java has the following conditional statements:
          "if" -> to specify a block of code to be executed, if a specified condition is true.
          "else" -> to specify a block of code to be executed, if a specified condition is false.
          "else if" -> to specify a new condition to test, if the first(last before itself) condition is false.
          "switch" -> to specify many alternative blocks of code to be executed.

     */

}
